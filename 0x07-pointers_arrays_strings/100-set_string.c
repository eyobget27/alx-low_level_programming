#include "main.h"
#include <stdio.h>

/**
* set_string - locates a character in a string
* @s: is a pointer of a pointer type char
* @to: is a pointer type char
* Return: nothing
*/
void set_string(char **s, char *to)
{
*s = to;
}
