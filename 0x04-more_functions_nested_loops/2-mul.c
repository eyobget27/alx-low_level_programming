#include "main.h"
/**
* mul - checks code for multiplication result
* a: number one
* b: number two
* result: multiplication result
* Return : Always 0
*/
int mul(int a, int b)
{
	int result;

	result = a * b;

	printf("%d", result);

	return (0);
}

